# 集合归属与资料库导入

状态:已接受

每个用户恰好拥有一个集合(Collection),由数据库唯一性约束保证。系统资料库(Library)是一个独立的、只读的集合,没有用户所有者。资料库导入会将牌组(Deck)和笔记(Note)复制到用户的集合中;导入的笔记保留其资料库 `guid`,因此重复导入是幂等的,但后续导入绝不会覆盖本地编辑。共享的参考媒体可以继续共享存放于 OSS,而用户录音保持集合内私有。

这为第一版提供了简单的授权与删除语义。教师访问权限、共享用户集合、作业(Assignment)以及资料库自动同步均推迟到后续版本。
